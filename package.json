{
  "name": "lightning-worker-contract-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-repo contract tests for the Lightning <-> ws-worker boundary",
  "scripts": {
    "stack": "bun src/cli.ts",
    "up": "bun src/cli.ts up",
    "down": "bun src/cli.ts down",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
